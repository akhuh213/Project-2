
<% if (data) { %>
    <% data.forEach(d => { %>
<% let calorie = d.food.nutrients.ENERC_KCAL  %> 
<% let calStr = calorie.toString() %>
<% let calPerSer = calStr.split('.',1).toString() %>
        <img src= "<%=d.food.image %>"> 
        <%= d.food.label %> </br>
        <label for="calPerSer">Calories per Serve :</label>
        <%= calPerSer%>
        <form action="/calorielog" method="post">
        <input hidden type="text" name="name" id="name" value="<%= d.food.label %>">
        <input hidden type="text" name="cal" id="cal" value="<%= calPerSer %>">
        <button type="submit" class="waves-effect waves-light btn">Add to Calorie Log</button>
        </form> 
    <% })} %> 

    <table class="striped">
        <thead>
          <tr>
              <th>Name</th>
              <th>Item Name</th>
              <th>Item Price</th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>Alvin</td>
            <td>Eclair</td>
            <td>$0.87</td>
          </tr>
          <tr>
            <td>Alan</td>
            <td>Jellybean</td>
            <td>$3.76</td>
          </tr>
          <tr>
            <td>Jonathan</td>
            <td>Lollipop</td>
            <td>$7.00</td>
          </tr>
        </tbody>
      </table>